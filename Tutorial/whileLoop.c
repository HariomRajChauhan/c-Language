#include<stdio.h>

int main(){
    int a=0;
//    scanf("%d",&a);

    while (a<=20)
    {
        if (a>=10)
        {
            printf("The Number between 10 and 20 is %d\n",a);
        }
        a++;
    }

    return 0;
}